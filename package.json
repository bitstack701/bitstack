{
  "name": "bitstack.co.za",
  "version": "0.1.0",
  "private": true,
  "now": {
    "name": "bitstack",
    "alias": "bitstack.co.za"
  },
  "dependencies": {
    "classnames": "^2.2.5",
    "express": "^4.14.1",
    "socket.io": "^1.7.2",
    "rxjs": "^5.0.3"
  },
  "scripts": {
    "start": "node server.js",
    "server": "DEBUG=bitstack:* node server.js",
    "client": "babel-node start-client.js",
    "build-client": "cd client; npm run build; cd ..",
    "deploy": "npm run build-client; now -e DEBUG=bitstack:* -e NODE_ENV=production;",
    "dev": "echo \"This command has been deprecated. Use 'npm start'\" && exit 1"
  }
}
